{
  "name": "sort-by-rating",
  "version": "1.0.0",
  "private": true,
  "description": "Browser extension that sorts Amazon and Flipkart results by ratings and review counts.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "icons": "node tools/make-icons.mjs",
    "build": "node build.mjs",
    "test": "node --test tests/",
    "all": "npm run icons && npm run build && npm test"
  },
  "devDependencies": {
    "jsdom": "^24.1.3"
  }
}
